cd ../lmbench/src
make

# Actually, lmbench additionally requires the user to configure lmbench.
# This can be done by running `make results` at ../lmbenchs/src, which will 
# also run the benchmark itself.
